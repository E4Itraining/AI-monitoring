global:
  scrape_interval: 5s

scrape_configs:
  # ... tes jobs existants (prometheus, tempo, etc.) ...

  - job_name: "ai-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:8000"

