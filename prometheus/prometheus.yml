global:
  scrape_interval: 5s

scrape_configs:
  # ... tes jobs existants (prometheus, tempo, etc.) ...

  - job_name: "ai-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "app:8000"

